# 유저플로우 v1.2

> 근거: `기능명세서_v2.md` §2 전체 흐름 + 각 기능 상세 · 작성일 2026-07-19 · 수정 2026-07-21
> **결정 전제(월요일 결정 회의에서 뒤집히면 이 문서도 수정):**
> D-01=C안(관심사 매칭 전용 수집·미노출) · D-07=CONTACT-01 통합(P0) · D-12=48시간 후 AI 단독 확정 · D-17=1회성 전달
>
> **v3 확정 반영(v1.1):**
> - **D-03 = "사랑의 온도" 단일 점수 체계**(`SURVEY-03` · §4.6, 기본 36.5도). v1.0의 "연습 레벨·경험치" 표기는 전부 폐기.
> - **D-21 = 매칭 근거 비공개 확정**(`MATCH-05` 제외 유지). 매칭 카드에 근거 문장을 노출하지 않는다.
>
> **⚠️ v1.2 — D-04 재결정 전제(명세 미반영 상태):**
> 본 문서는 **AI 화상 트랙(F6)을 P1로 채택한 안**을 그린다. 그러나 `기능명세서_v3.md`의 현행 확정은
> **D-04 = AI 화상 트랙 P2 보류**(부록 A `AI-DATE-02`(구) 제외)이다. **F6은 아직 승인되지 않은 제안**이며,
> D-04를 재논의해 뒤집기 전까지는 이 문서가 명세를 앞서 있다. 회의에서 부결되면 **F6 전체와 F2의 AI 화상 분기를 되돌린다.**
>
> 표기: `[화면]` = 와이어프레임 화면 번호(`와이어프레임.html` 참조) · ◆ = 분기 · ⚠ = 이탈/실패 경로

---

## 0. 전체 서비스 맵

```mermaid
flowchart LR
    A[온보딩<br/>F1] --> B[홈·매칭<br/>F2]
    B -->|실사용자 트랙| C[화상 세션<br/>F3]
    B -->|"AI 화상 트랙 (F6)"| F["AI 소개팅 연습<br/>이모지+TTS<br/>F6"]
    B -->|AI 챗봇 트랙| E[챗봇 대화<br/>F5]
    C --> D[세션 후<br/>평가·리포트·교환<br/>F4]
    D --> B
    E --> B
    F --> B
    B -.->|"⚠ 매칭 지연·취소·상대 노쇼 시 대체재"| F
```

---

## F1. 온보딩 — 가입부터 매칭 가능 상태까지

```mermaid
flowchart TD
    S([방문]) --> L["랜딩·로그인 [01]<br/>AUTH-01"]
    L -->|기존 회원| HOME([홈으로])
    L -->|신규| R["회원가입 [02]<br/>이메일/소셜 · AUTH-01"]
    R --> AGE{"성인 확인<br/>AUTH-02"}
    AGE -->|미성년| BLOCK[/"⚠ 가입 차단 안내<br/>서비스 이용 불가"/]
    AGE -->|성인| CONSENT["목적별 동의 [03]<br/>AUTH-03"]

    CONSENT --> CGATE{"필수 약관<br/>동의?"}
    CGATE -->|거부| BLOCK2[/"⚠ 가입 진행 불가<br/>(필수 항목)"/]
    CGATE -->|동의| OPT["선택 동의 플래그 저장<br/>얼굴 · 표정 · 음성 · 리포트 (각각 독립)<br/>마이페이지 [19]에서 언제든 철회"]

    OPT --> PROF["기본 프로필 [04]<br/>PROFILE-01"]

    PROF --> FACE{"얼굴 분석<br/>플래그 = 동의?"}
    FACE -->|미동의| SURVEY
    FACE -->|동의| CAM["실시간 얼굴 촬영 [05]<br/>PROFILE-02"]
    CAM --> CAMOK{촬영 검증}
    CAMOK -->|"⚠ 미감지/다중/가림/조명 부족"| RETRYC{"재시도 3회<br/>초과?"}
    RETRYC -->|아니오| CAM
    RETRYC -->|"⚠ 초과 → 건너뛰기"| SKIPF[/"얼굴상 태그 없이 진행<br/>마이페이지에서 재촬영 가능"/]
    SKIPF --> SURVEY
    CAMOK -->|통과| TAG["얼굴상 태그 결과 [05b]<br/>PROFILE-03 · 공개 여부 선택"]
    TAG --> SURVEY["설문 [06]<br/>이상형 SURVEY-01 · 성향 SURVEY-02<br/>연습 목표 PROFILE-05"]

    SURVEY --> TIME["가능 시간대 입력 [07]<br/>MATCH-02-1 · 주 단위"]
    TIME --> TEMP["사랑의 온도 36.5도 부여<br/>SURVEY-03 · §4.6 · 사전 진단 설문 없음"]
    TEMP --> HOME
```

**핵심 규칙**
- 선택 동의를 **모두 거부해도** 기본 화상 연습(홈 진입·매칭·세션)은 가능하다. 거부 시 해당 AI 분석 기능만 비활성(세션 중 코칭 미제공, 리포트는 지표 기반 축소판).
- 얼굴 분석 거부 시 얼굴상 태그 없음 → 매칭의 "선호 분위기 적합도"는 키·자기소개만으로 계산.
- **선택 동의 4종은 각각 독립 플래그**이며, 이후 플로우에서 아래와 같이 소비된다. 동의 화면에서 분기시키지 않고 **플래그를 저장한 뒤 사용 시점마다 게이트**한다.

| 플래그 | 소비 지점 | 미동의 시 |
|---|---|---|
| 얼굴 | F1 `FACE` 게이트 | 촬영·얼굴상 태그 생략 |
| 표정 | F3 `CGATE2` 게이트 | 표정·시선 코칭(COACH-01) 미제공 |
| 음성 | F3 `CGATE2` 게이트 | 대화 흐름 코칭(COACH-02)·발화 지표 미산출 |
| 리포트 | F4 리포트 생성 | 리포트 축소판(수치 지표만, 행동 근거 인용 없음) |

- 온보딩 중 이탈 시 **입력분은 임시 저장**하고, 재로그인 시 마지막 단계부터 재개한다. 온도 부여(`TEMP`)까지 완료해야 매칭 가능 상태가 된다.

---

## F2. 매칭 — 트랙 선택부터 세션 확정까지

```mermaid
flowchart TD
    HOME(["홈 [08]"]) --> TRACK{"매칭 트랙 선택<br/>MATCH-01"}
    TRACK -->|"AI 화상 연습 (이모지+TTS)"| AIV([F6 AI 화상으로])
    TRACK -->|"AI 챗봇 (텍스트)"| BOT([F5 챗봇으로])
    TRACK -->|실사용자| Q["대기 큐 등록<br/>※ 동시 진행 매칭 1개 제한"]

    Q --> FILTER["필수 조건 필터 MATCH-02<br/>상호 대상·연령·시간대 교집합<br/>차단/정지/최근 매칭 제외"]
    FILTER --> SCORE["매칭 점수 산출 MATCH-03<br/>목표30·성향25·관심사20·일정15·분위기10"]
    SCORE --> FOUND{후보 발견?}
    FOUND -->|"⚠ 지연"| WAIT["지연 원인 안내<br/>'시간대를 넓히면 빨라져요'"]
    WAIT --> STAY{"대기 계속?"}
    STAY -->|계속 대기| FILTER
    STAY -->|"조건 완화"| EDITC["가능 시간대·조건 수정<br/>[07] 재진입"] --> FILTER
    STAY -->|"◆ 큐 이탈"| LEAVE[/"큐 등록 해제<br/>패널티·온도 감점 없음"/] --> HOME2([홈으로])
    STAY -->|"◆ AI 화상 전환 (대기 유지)"| AIV
    STAY -->|"◆ 챗봇 전환"| BOT

    FOUND -->|성립| CARD["매칭 카드 [09]<br/>닉네임·연령대·키·얼굴상<br/>※ 매칭 근거 비공개 — MATCH-05 제외(D-21)"]

    CARD --> ACCEPT{"양측 수락?<br/>MATCH-06 · 제한 시간 내"}
    ACCEPT -->|"⚠ 한쪽 거절/시간 초과"| Q
    ACCEPT -->|양측 수락| FIX["세션 확정<br/>NOTIFY-01: 확정 + 취소 정책 안내"]

    FIX --> PRE["세션 대기 상태<br/>확정 ~ 세션 시작 직전<br/>리마인드 2시간 · 1시간 · 10분 전"]

    PRE -.->|"◆ 내가 취소 (1시간 이전)"| C1[/"사랑의 온도 소폭 감소<br/>노쇼 패널티 없음"/]
    PRE -.->|"◆ 내가 취소 (1시간 이내)"| C2[/"사랑의 온도 대폭 감소<br/>노쇼 패널티 1회"/]
    C1 --> REQ{"바로 재매칭?"}
    C2 --> REQ
    REQ -->|예| Q
    REQ -->|아니오| HOME2

    PRE -.->|"◆ 상대 취소 (알림 수신)"| ALT{"대체 제공"}
    ALT -->|우선 재매칭| Q
    ALT -->|"AI 화상 즉시 연습"| AIV
    ALT -->|챗봇 전환| BOT
    ALT -->|"나중에"| HOME2

    PRE --> T(["세션 시각 도달"])
    T --> ENTER{"5분 내 입장 현황<br/>MODERATION-04"}
    ENTER -->|양측 입장| ROOM([F3 대기방 입장])
    ENTER -->|"⚠ 내가 미입장"| NOSHOW[/"내 노쇼 처리<br/>온도 차감 + 패널티 1회<br/>(6개월 단위 소멸)"/] --> HOME2
    ENTER -->|"⚠ 상대 미입장"| VICT["상대 노쇼 안내<br/>내 온도·패널티 영향 없음"]
    VICT --> VOPT{"보상 선택"}
    VOPT -->|우선 재매칭| Q
    VOPT -->|"AI 화상 즉시 연습"| AIV
    VOPT -->|챗봇 전환| BOT
    VOPT -->|종료| HOME2
```

**핵심 규칙**
- **취소는 확정 직후부터 세션 시작 전까지 언제든 가능**하다. 리마인드 발송 여부와 무관하며, 패널티는 오직 **세션 시작 1시간 기준**으로만 갈린다(§6.2.1).
- 취소·노쇼 후 **자동으로 큐에 재등록하지 않는다.** 재매칭 여부는 항상 사용자가 선택한다.
- **상대 노쇼·상대 취소는 내 온도와 노쇼 패널티에 영향을 주지 않는다.** 피해자에게는 우선 재매칭 또는 챗봇 전환을 제공한다.
- 큐 대기 중 이탈은 언제든 가능하며 패널티가 없다. 이탈 시 `동시 진행 매칭 1개 제한` 슬롯도 함께 해제된다.
- **AI 화상 전환(F6)은 대기 큐를 유지한 채 진행**한다. 연습 도중 매칭이 성립하면 알림만 띄우고, **수락 여부는 AI 세션 종료 후에 묻는다**(세션 중 강제 이탈 없음). 반면 챗봇 전환(F5)·큐 이탈은 슬롯을 해제한다.

---

## F3. 화상 세션 — 대기방부터 종료까지 (화면 테마: 다크 고정)

```mermaid
flowchart TD
    IN([입장]) --> ROOM["상황형 대기방 [11]<br/>ROOM-01 시간대별 장소 · ROOM-03 안내<br/>기기 점검 · 동의 상태 확인 · 세션 목표"]
    ROOM --> GATE{"상호 동의 게이트<br/>기기 정상?"}
    GATE -->|"⚠ 장치 오류"| DEV[/"점검 안내 · 재시도"/] --> ROOM
    GATE -->|통과| RTC["WebRTC 연결 SESSION-01"]
    RTC --> CONN{연결 성공?}
    CONN -->|"⚠ 실패"| RETRY{"재시도 3회<br/>초과?"}
    RETRY -->|아니오| RTC
    RETRY -->|"⚠ 초과"| ABORT[/"세션 취소 — 양측 모두 노쇼 아님<br/>온도 감점 없음 · 재매칭 안내"/]
    ABORT --> RQ([F2 큐로])
    CONN -->|성공| LIVE

    subgraph LIVE["30분 세션 루프 [12] · SESSION-02 타이머"]
        direction TB
        TALK[대화 진행] --> CGATE2{"표정·음성 분석<br/>동의 플래그?"}
        CGATE2 -->|동의| COACH["실시간 코칭 (본인 전용)<br/>COACH-01 표정·시선 · COACH-02 대화 흐름<br/>강도·주기 COACH-04"]
        CGATE2 -->|미동의| NOCOACH["코칭 미제공<br/>(리포트도 축소판)"]
        COACH --> TALK
        NOCOACH --> TALK
        TALK --> SIL{"침묵 감지 SILENCE-01"}
        SIL -->|"0–10초"| TALK
        SIL -->|"15–20초"| TB["주제 버튼 표시"] --> TALK
        SIL -->|"30초"| QC["범용 질문 카드 3개<br/>(LLM 호출 없음)"] --> TALK
        SIL -->|"45초+"| RAGQ["맥락 기반 질문 3개<br/>SILENCE-03(P1)"] --> TALK
        TALK --> HELP["도움 버튼 SILENCE-02<br/>(수동 커닝페이퍼)"] --> TALK
        TALK --> BAD["부적절 표현 감지 SAFETY-01(P1)<br/>본인에게만 완곡 경고"] --> TALK
    end

    LIVE -->|"타이머 종료 (10·5·1분 전 알림)"| END
    LIVE -.->|"◆ 조기 종료 버튼"| CONFIRM{확인 모달}
    CONFIRM -->|"취소 — 세션 계속"| LIVE
    CONFIRM -->|종료| END
    LIVE -.->|"◆ 신고 [13] MODERATION-01"| REP{심각 유형?}
    REP -->|아니오| LIVE
    REP -->|"예 — 즉시 종료"| ENDR["신고 종료 처리<br/>연결 강제 종료 · 지표 집계 · 원본 영상 삭제<br/>신고 건 운영 검토 MODERATION-03"]

    END["세션 종료 처리 RESULT-01<br/>연결 종료 · 지표 집계 · 원본 영상 삭제"] --> AFTER([F4 정상 종료 경로로])
    ENDR --> AFTERR([F4 신고 종료 경로로])
```

**핵심 규칙**
- **표정·음성 동의 플래그는 세션 진입 시 1회 평가**한다(`CGATE2`). 세션 중 변경되지 않으며, 미동의자는 코칭 없이 대화만 진행한다.
- 조기 종료 확인 모달에서 **취소하면 세션이 그대로 이어진다.** 조기 종료는 노쇼가 아니며, 진행된 구간까지의 지표로 리포트를 생성한다.
- WebRTC 연결이 3회 실패하면 **양측 모두 노쇼·온도 감점 없이** 세션을 취소한다(귀책이 사용자에게 없음).
- **심각 유형 신고로 종료된 세션은 상호 평가·5분 연장·연락처 교환을 건너뛴다**(F4 신고 종료 경로). 가해자에게 평가·연장 기회를 주지 않기 위함이다.

---

## F4. 세션 후 — 평가 · 리포트 · 연락처 교환

```mermaid
flowchart TD
    S([정상 종료]) --> EVAL["상호 평가 입력 [14]<br/>RESULT-02 · 6항목 1~5점 + 서술(선택)"]
    S -.->|"◆ 신고/차단 선택"| BLK
    SR([신고 종료]) --> BLK["차단 MODERATION-02<br/>→ 이후 매칭 상호 제외"]
    BLK --> SKIPE["평가 · 5분 연장 · 연락처 교환 전부 생략"]
    SKIPE --> GENR["AI 분석 단독 리포트 [16]<br/>상대 평가 미수집 · 후순위 패널티 미적용"]
    GENR --> TEMPU

    EVAL --> SUBMIT{내 평가 제출?}
    SUBMIT -->|"⚠ 미제출"| LOCK["상대 평가 영역 잠금<br/>(상호성 게이트)<br/>내 리포트는 AI 분석만 표시"]
    LOCK --> LATER{"이후 제출?"}
    LATER -->|"제출 (알림 재발송)"| EVAL
    LATER -->|"⚠ 48시간 내 미제출"| PENAL[/"상대 평가 영구 미열람<br/>+ 매칭 후순위"/]
    PENAL --> WAITP
    SUBMIT -->|제출| EXT{"5분 연장 의사?<br/>CONTACT-01"}

    EXT -->|"양측 승인"| FIVE["5분 화상 연장 [15]"]
    EXT -->|"한쪽이라도 거절<br/>(거절 여부 비노출)"| WAITP
    FIVE --> SHARE{"연락처 공개 동의?<br/>CONTACT-02"}
    SHARE -->|양측 동의| OPEN["인스타/카톡 ID 상호 공개<br/>1회성 전달(D-17) + 알림"]
    SHARE -->|"한쪽 거절<br/>(거절 여부 비노출)"| WAITP
    OPEN --> WAITP

    WAITP["AI 분석 집계 완료<br/>상대 평가 도착 대기 (최대 48시간)"]
    WAITP --> PEER{"상대 평가 도착?"}
    PEER -->|도착| GEN
    PEER -->|"⚠ 48시간 초과 (D-12)"| AIONLY["AI 분석만으로 확정<br/>미제출자는 매칭 후순위"]
    AIONLY --> GEN

    GEN["AI 리포트 생성 → 알림 [16]<br/>REPORT-01 레이더 · 01-1 이슈 맥락<br/>02 행동 근거 · 04 요약<br/>상대 평가 도착 시 AI vs 상대 비교 포함"]
    GEN --> TEMPU["사랑의 온도 갱신<br/>SURVEY-03 · §4.6<br/>상호 평가 + AI 피드백 + 매너 감점"]
    TEMPU --> DASH["성장 대시보드 반영 [17]<br/>GROWTH-01 히스토리"]
    DASH --> HOME([홈으로 · 다음 세션])

    AIONLY -.->|"48시간 이후 상대가 제출"| NOUPD[/"리포트·온도 재산정 없음<br/>확정본 유지"/]
```

**핵심 규칙**
- **리포트 생성은 단 한 번**이다. `WAITP`에서 상대 평가를 최대 48시간 대기한 뒤 확정 생성하며, "먼저 만들고 나중에 완성판을 다시 만드는" 2단계가 아니다. 대기 중에는 AI 분석 진행 상태만 노출한다.
- **48시간 경과 후 확정된 리포트는 상대가 뒤늦게 제출해도 재산정하지 않는다**(`NOUPD`). 온도도 갱신하지 않는다.
- **상호성 게이트는 영구 잠금이 아니다.** 미제출 상태에서도 내 AI 분석 리포트는 열람 가능하며, 48시간 내 제출하면 상대 평가가 열린다. 미제출로 확정되면 상대 평가는 영구 미열람 + 매칭 후순위.
- **신고 종료·차단 경로는 평가·연장·연락처 단계를 건너뛰고 곧바로 AI 단독 리포트로 간다.** 이 경로에서는 신고자에게 온도 감점을 적용하지 않으며, 피신고자 제재는 `MODERATION-03` 운영 검토를 따른다.

---

## F5. AI 챗봇 트랙 (텍스트 전용 · 화상 없음)

```mermaid
flowchart TD
    S([트랙 선택: AI]) --> PHASE{"연습 단계 선택"}
    PHASE -->|"소개팅 전"| PREC["사전 대화 시나리오<br/>첫 인사 · 호감 표현<br/>약속 일시·장소 정하기<br/>→ 오프닝 프롬프트 A"]
    PHASE -->|"소개팅 후"| POSTC["애프터 대화 시나리오<br/>세션 후 대화 이어가기<br/>재만남 제안<br/>→ 오프닝 프롬프트 B"]
    PREC --> SETUP
    POSTC --> SETUP["페르소나 설정 [10]<br/>AI-DATE-02 · 말투 중심<br/>+난이도·성향·반응·주제"]
    SETUP --> CHAT["챗봇 대화 AI-DATE-01<br/>즉각 반응형 · 24시간"]
    CHAT --> IDLE{"12시간 무응답?"}
    IDLE -->|아니오| CHAT
    IDLE -->|"예 (1회 한정)"| PING{"현재 야간<br/>00–09시?"}
    PING -->|"예 (D-05)"| HOLD[/"발송 보류<br/>아침까지 대기"/] --> FIRST
    PING -->|아니오| FIRST["시간대별 선톡 1회 + 알림<br/>재발송 없음"]
    FIRST --> CHAT
    CHAT -->|대화 종료| HOME([홈으로])
```

---

## F6. AI 화상 소개팅 연습 (이모지 + TTS) — *D-04 재결정 전제 · P1 제안*

> **트랙 정의:** AI 상대가 **TTS 음성 + 이모지 리액션**으로 소개팅 상황을 연출하고, 사용자는 **음성으로 답변**한다.
> 아바타·얼굴 렌더링은 없다(`AI-DATE-03` 제외 유지). 화면 테마는 F3와 동일하게 **다크 고정**.
>
> **존재 이유:** 매칭 지연·상대 취소·상대 노쇼 시 **동급의 대체재**를 제공하고, 신규 사용자의 첫 경험을 "대기 화면"이 아닌 "즉시 연습"으로 만든다.

```mermaid
flowchart TD
    S([트랙 선택: AI 화상]) --> PRECHK{"음성 분석 동의<br/>+ 마이크 정상?"}
    PRECHK -->|"⚠ 마이크 오류"| DEVF[/"기기 점검 안내 · 재시도"/] --> PRECHK
    PRECHK -->|"⚠ 음성 미동의"| NOV[/"AI 화상 트랙 이용 불가<br/>(음성 입력이 필수 조건)"/] --> BOT([F5 챗봇 트랙 안내])

    PRECHK -->|통과| CAMOPT{"내 카메라 켜기?<br/>표정 동의 플래그"}
    CAMOPT -->|"켬 + 표정 동의"| FULLM["풀 모드<br/>COACH-01 표정·시선 + COACH-02 대화 흐름"]
    CAMOPT -->|"끔 또는 표정 미동의"| VOICEM["음성 전용 모드<br/>COACH-02 대화 흐름만"]
    FULLM --> SET
    VOICEM --> SET["시나리오·난이도 설정<br/>상황(첫 만남·카페·식사 후)<br/>난이도(편안함/보통/실전)<br/>세션 목표 = PROFILE-05 연동"]

    SET --> ROOM["AI 대기방<br/>기기 점검 · 진행 방식 안내<br/>※ '상대는 실제 사람이 아닙니다' 명시 고지"]
    ROOM --> START["세션 시작 · 10~15분<br/>SESSION-02 타이머 재사용"]
    START --> LIVE

    subgraph LIVE["AI 세션 루프 · 10~15분"]
        direction TB
        AITALK["AI 발화<br/>TTS 음성 + 이모지 리액션<br/>😊 호의 · 😐 중립 · 🤔 곤란"]
        AITALK --> MYTURN["내 발화 (음성)<br/>실시간 STT 인식"]
        MYTURN --> ANALYZE["지표 산출 (F3와 동일 파이프라인)<br/>발화 비율·질문 횟수·후속 질문<br/>침묵 시간·말하기 속도"]
        ANALYZE --> COACH["실시간 코칭 (모드에 따라)<br/>COACH-04 강도·주기"]
        COACH --> AITALK
        MYTURN --> SIL{"침묵 감지<br/>SILENCE-01 재사용"}
        SIL -->|"0–10초"| AITALK
        SIL -->|"15초+"| PROMPT["AI가 먼저 질문<br/>(침묵 개입을 AI 발화로 대체)"] --> AITALK
        MYTURN --> HELP["도움 버튼 SILENCE-02"] --> AITALK
        MYTURN --> BADA["부적절 표현 감지 SAFETY-01<br/>본인에게만 완곡 경고"] --> AITALK
    end

    LIVE -->|"타이머 종료 (5·1분 전 알림)"| END
    LIVE -.->|"◆ 조기 종료 버튼"| CONFIRM{확인 모달}
    CONFIRM -->|"취소 — 세션 계속"| LIVE
    CONFIRM -->|종료| END
    LIVE -.->|"⚠ 실사용자 매칭 성립 알림"| TOAST[/"알림만 표시 · 세션 유지<br/>수락 여부는 종료 후 확인"/] --> LIVE

    END["세션 종료 처리<br/>지표 집계 · 음성 원본 즉시 삭제"] --> REP2["AI 리포트 [16 변형]<br/>REPORT-01 레이더 — AI 분석 단독<br/>02 행동 근거 · 04 요약<br/>※ 상대 평가 축 없음"]
    REP2 --> TEMPU2["사랑의 온도 갱신<br/>SURVEY-03 · §4.6<br/>⚠ AI 트랙 반영 상한 적용 (아래 규칙)"]
    TEMPU2 --> DASH2["성장 대시보드 [17]<br/>GROWTH-01 · AI 트랙 별도 표기"]

    DASH2 --> PEND{"대기 중 성립한<br/>매칭 있나?"}
    PEND -->|있음| ACC([F2 매칭 카드 수락 판단])
    PEND -->|없음| NEXT{"이어서?"}
    NEXT -->|"다시 연습"| SET
    NEXT -->|"실사용자 매칭"| Q([F2 큐로])
    NEXT -->|종료| HOME([홈으로])
```

**핵심 규칙**

- **음성 분석 동의는 이 트랙의 필수 조건**이다. 미동의자는 입력 수단이 없으므로 트랙 자체를 이용할 수 없고, 텍스트 챗봇(F5)으로 안내한다. (F1 플래그 표의 "음성" 항목 소비 지점이 하나 늘어난다.)
- **카메라는 선택**이다. 켜면 풀 모드로 `COACH-01` 표정·시선 코칭이 붙는다. 단 상대가 이모지이므로 **"시선 유지"의 기준은 "상대 얼굴 응시"가 아니라 "화면 응시"** 로 완화한다 — F3의 시선 지표와 **동일 축으로 비교하면 안 된다.**
- **리포트에는 상대 평가 축이 없다.** `REPORT-01` 레이더는 AI 분석 단독으로 그리며, F3/F4 리포트와 겹쳐 보여줄 때 **비교 가능 지표(발화 비율·질문 횟수·침묵)와 비교 불가 지표(매너·상대 체감)를 시각적으로 분리**한다.
- **음성 원본은 세션 종료 즉시 삭제**한다(§22 저장 원칙). 지표와 리포트만 남긴다.
- **AI임을 명시 고지**한다. 대기방과 세션 화면 모두에 "상대는 실제 사람이 아닙니다"를 상시 노출한다.
- 실사용자 매칭이 세션 도중 성립하면 **알림만 띄우고 세션을 끊지 않는다.** 수락 판단은 종료 후 `PEND`에서 한다.

**⚠️ 미결정 — 온도 반영 상한 (신규)**

AI 세션 결과를 사랑의 온도에 **반영하기로 결정**했으나, AI 상대는 **무한 반복이 가능**하므로 상한 없이 반영하면 온도가 실력 지표로서 의미를 잃는다(파밍). 아래 중 하나를 확정해야 한다.

| 안 | 내용 | 비고 |
|---|---|---|
| **A (권장)** | **일/주 단위 반영 횟수 상한** (예: 주 3회까지만 온도 반영, 초과분은 리포트만 제공) | 파밍 차단 + 연습 동기 유지 |
| B | AI 세션은 **가중치 축소 반영** (실사용자 세션의 30~50%) | 상한 관리 불필요하나 체감이 모호 |
| C | AI 세션은 **가점만, 감점 없음** | 연습 부담은 낮으나 단조 상승 |

> 상한값·가중치 수치는 D-03의 잔여 항목(산정식 가중치)과 함께 확정해야 한다.

**F3(실사용자)와의 차이 요약**

| 항목 | F3 실사용자 | F6 AI 화상 |
|---|---|---|
| 세션 길이 | 30분 | **10~15분** |
| 상대 표현 | 실시간 영상 | **TTS 음성 + 이모지** |
| 내 입력 | 음성 + 영상 | **음성** (영상은 선택) |
| 침묵 개입 | 4단계(주제 버튼→질문 카드→RAG) | **AI가 직접 질문**(1단계) |
| 코칭 | COACH-01/02 | COACH-02 (+ 카메라 시 01) |
| 세션 후 | 상호 평가 → 연장 → 연락처 → 리포트 | **리포트 직행** (평가·연장·연락처 없음) |
| 온도 반영 | 전량 | **상한 적용** (위 미결정) |
| 노쇼·패널티 | 있음 | **없음** |

---

## 부록: 분기 커버리지 체크리스트

`개발착수_권장사항.md` §1.1에서 요구한 분기가 모두 포함됐는지 확인용.

| 분기 | 위치 | 상태 |
|---|---|---|
| 미성년 차단 | F1 | ✅ |
| 선택 동의 거부 (기능 축소 경로) | F1 | ✅ |
| 얼굴 촬영 실패 (미감지·다중·가림·조명) | F1 | ✅ |
| 매칭 지연 안내 | F2 | ✅ |
| 수락 거절/시간 초과 → 큐 재등록 | F2 | ✅ |
| 취소 시점별 패널티 (1시간 기준) | F2 | ✅ |
| 상대 취소 → 대체 매칭/챗봇 전환 | F2 | ✅ |
| 노쇼 (5분 대기) | F2 | ✅ |
| WebRTC 연결 실패 재시도 | F3 | ✅ |
| 침묵 4단계 개입 | F3 | ✅ |
| 조기 종료 / 세션 중 신고 | F3 | ✅ |
| 상호성 게이트 (평가 미제출 잠금) | F4 | ✅ |
| 상대 48시간 미제출 → AI 단독 확정 | F4 | ✅ |
| 5분 연장/연락처 한쪽 거절 (거절 비노출) | F4 | ✅ |
| 챗봇 선톡 1회 제한 + 야간 보류 | F5 | ✅ |

**v1.1에서 추가된 분기** — v1.0 체크리스트가 "반대편 당사자·복구 경로"를 누락하고 있었음.

| 분기 | 위치 | 상태 |
|---|---|---|
| 얼굴 촬영 3회 실패 → 건너뛰기 | F1 | ✅ |
| 표정·음성 미동의 → 코칭 미제공 경로 | F3 | ✅ |
| 큐 대기 이탈 / 조건 완화 재검색 | F2 | ✅ |
| 취소·노쇼 후 재매칭 여부 사용자 선택 | F2 | ✅ |
| **상대 노쇼 시 피해자 경로**(무패널티 + 보상) | F2 | ✅ |
| WebRTC 3회 실패 → 양측 무귀책 취소 | F3 | ✅ |
| 조기 종료 모달 취소 → 세션 복귀 | F3 | ✅ |
| **신고 종료 → 평가·연장·연락처 생략 경로** | F3·F4 | ✅ |
| 평가 잠금 해제(48시간 내 지연 제출) | F4 | ✅ |
| 차단 이후 리포트 처리 | F4 | ✅ |
| 48시간 확정 후 지각 제출 → 재산정 없음 | F4 | ✅ |
| 챗봇 연습 단계별 시나리오 분기 | F5 | ✅ |

**v1.2에서 추가된 분기** — AI 화상 트랙(F6) 신설분. *D-04 재결정 전제.*

| 분기 | 위치 | 상태 |
|---|---|---|
| 트랙 3분기 (실사용자 / AI 화상 / AI 챗봇) | F2 | ✅ |
| 매칭 지연 → AI 화상 전환 (큐 유지) | F2·F6 | ✅ |
| 상대 취소·상대 노쇼 → AI 화상 즉시 연습 | F2·F6 | ✅ |
| 음성 미동의 → 트랙 이용 불가 → 챗봇 안내 | F6 | ✅ |
| 마이크 오류 → 점검·재시도 | F6 | ✅ |
| 카메라 ON/OFF → 풀 모드 / 음성 전용 모드 | F6 | ✅ |
| AI 세션 중 매칭 성립 → 알림만, 종료 후 수락 판단 | F6 | ✅ |
| AI 세션 조기 종료 + 모달 취소 복귀 | F6 | ✅ |
| AI 침묵 개입 (AI가 먼저 질문) | F6 | ✅ |
| AI 리포트 (상대 평가 축 없음) | F6 | ✅ |
| 온도 반영 상한 | F6 | ⚠️ **미결정** |
