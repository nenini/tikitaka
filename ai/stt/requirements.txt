# ===== AI 파트 의존성 (S15P11A307) =====
# 소스 오브 트루스는 pyproject.toml + uv.lock. 이 파일은 참고·Docker 빌드용.
# 설치:  uv sync   (또는  pip install -r requirements.txt)
# Python 3.12 기준 (시스템 기본 3.14는 ML 라이브러리 호환 문제로 사용하지 않음)

# --- 현재 사용 (STT 입력단) ---
faster-whisper==1.2.1          # STT (ctranslate2·onnxruntime 자동 동반)
sounddevice==0.5.5             # 마이크 캡처
numpy==2.5.1
pydantic==2.13.4               # 이벤트 스키마 (TranscriptEvent)

# --- GPU (CUDA 12 / cuDNN 9, Windows) — faster-whisper(CTranslate2)용 ---
nvidia-cublas-cu12==12.9.2.10
nvidia-cudnn-cu12==9.24.0.43

# --- 앞으로 사용 (환경세팅 · AI 서버) ---
fastapi                        # AI 서비스 API
uvicorn[standard]              # ASGI 서버
pydantic-settings              # .env / 시크릿 관리
httpx                          # LLM/STT 게이트웨이(GMS) 호출 어댑터
python-multipart               # 파일 업로드 (사진 / 얼굴상)

# --- 개발 도구 ---
pytest==9.1.1
ruff                           # 린트 / 포맷 (예정)

# --- 선택 · 도전 (P2, 필요 시 주석 해제) ---
# mem0ai                       # 세션 간 누적 기억 (+ pgvector)
# noisereduce                  # 노이즈 제거 (STT엔 역효과 가능, 신중)
# 로컬 LLM(챗봇): transformers / vllm / llama-cpp-python 중 미정
