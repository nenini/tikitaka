/**
 * 시·도 목록 (행정 표준 17개).
 *
 * 온보딩 프로필 입력(W-04)과 지역 수정(W-19b)이 **같은 목록**을 써야 한다.
 * 한쪽에만 값을 추가하면 기존 사용자의 지역이 목록에 없어 `<select>` 가
 * 빈 값으로 보이므로, 여기 한 곳에서만 관리한다.
 *
 * 구·군은 수집하지 않는다(정확한 지역 미노출 · D-08).
 * 서버는 `regionCity` 를 자유 문자열(≤50자)로 받으므로 검증은 화면 몫이다.
 */
export const SIDO = [
  '서울특별시',
  '부산광역시',
  '대구광역시',
  '인천광역시',
  '광주광역시',
  '대전광역시',
  '울산광역시',
  '세종특별자치시',
  '경기도',
  '강원특별자치도',
  '충청북도',
  '충청남도',
  '전북특별자치도',
  '전라남도',
  '경상북도',
  '경상남도',
  '제주특별자치도',
] as const

export type Sido = (typeof SIDO)[number]
