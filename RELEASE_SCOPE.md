# 출시 범위 재확정 제안 (A5)

작성 2026-08-04 · 작성자 FE · **PO 승인 필요**
근거: `FE_총괄_업무_체크리스트.xlsx` 74건 전수 + 백엔드 컨트롤러 전수 조사

체크리스트 항목 **A5(출시 범위 P0/P1 재확정)** 의 산출물이다.
FE가 두 명에서 **한 명 체제**로 바뀌었으므로, "모두 P0" 상태로는 계획이 성립하지 않는다.
이 문서는 **무엇을 빼야 하는지에 대한 제안**이며, 결정은 PO가 한다.

---

## 1. 지금 숫자

| 우선순위 | 남은 건수 | 대략 공수 |
|---|---|---|
| P0-긴급 | 6건 | 9일 |
| **P0** | **32건** | **67.5일** |
| P1 | 21건 | 29일 |
| P2 | 3건 | 12.5일 |
| **합계** | **62건** | **약 118일** |

> 공수는 규모 태그를 인일로 환산한 **거친 추정**이다(XS 0.25 · S 0.5 · M 1.5 · L 3 · XL 6).
> 정확도를 주장하는 숫자가 아니라 **규모 감각**을 위한 것이다.

**핵심 문제는 P0 32건 / 67.5일이다.** 한 명이 처리하면 순수 개발만 **13주**가 넘는다.
남은 스프린트가 그보다 짧다면 P0 정의가 틀린 것이다 — 우선순위가 아니라 희망 목록이 돼 있다.

---

## 2. 제안 — 3단 분류

"P0/P1" 두 칸으로는 부족하다. **데모에서 실제로 보여줄 것**을 기준으로 나눈다.

### 🟢 MUST — 이게 없으면 데모가 성립하지 않는다

핵심 여정 **가입 → 온보딩 → 매칭 → 세션 → 평가 → 리포트** 한 줄을 끝까지 통과시키는 것.

| 단계 | 항목 | 상태 |
|---|---|---|
| 온보딩 | 동의·프로필·얼굴·설문 | ✅ 완료 (검증만 남음) |
| 온보딩 | 온보딩 재개·가드 | 🟠 부분 (판정 기준 완화 상태) |
| 매칭 | 전체 흐름 회귀 테스트 | 🔴 미실시 |
| 세션 | 대기방·LiveKit 회귀 테스트 | 🔴 미실시 |
| 세션 후 | 상호 평가 | ✅ 완료 (검증만 남음) |
| 세션 후 | **세션 리포트** | 🔴 **서버 미구현** |
| 공통 | 데모·폴백 제거(리포트·성장) | 🔴 |
| 공통 | 소셜 가입자 매칭 불가 수정 | 🔴 **기능이 죽어 있음** |

**MUST 안에 서버 미구현이 하나 있다 — 리포트.** 이게 안 오면 여정의 마지막 칸이 빈다.
서버가 못 맞추면 **"리포트 준비중" 화면으로 마감**하는 것을 대안으로 확정해야 한다
(가짜 점수를 보여주는 지금 상태는 어느 쪽이든 안 된다).

### 🟡 SHOULD — 있으면 완성도가 오르지만 없어도 데모는 돈다

- 비밀번호 재설정 화면 (P0)
- 알림 REST·SSE 연동 (P0) — UI 셸은 이미 있고 데모 고정 데이터
- 회원 탈퇴 · 개인정보 허브 실데이터 · 프로필/지역 수정 (P0)
- 코칭 카드 TTL·스택 (P0)
- 로딩·빈 상태·오류 표준화 (P0)
- 개발용·데모 경로 정리 (P0)

### 🔴 CUT — 이번 출시에서 뺀다

| 항목 | 현재 | 뺄 근거 |
|---|---|---|
| **AI 화상 트랙** (AIVIDEO-01~04) | P1 | 포함 시 **XL**. AI TTS 파이프라인이 없다. 한 명 체제에서 감당 불가 |
| **관리자 전체** (ADMIN) | P1 | 데모 여정에 등장하지 않는다. 스텁만 정리하고 종료 |
| **사진 스타일링 피드백** (STYLE) | P2 | XL 2건. 별도 동의·원본 삭제 정책까지 필요 |
| **성장 대시보드** (GROWTH) | P1 | 서버 미구현. 폴백만 안전화하고 '준비중' |
| **리포트 목록 화면** | P1 | 서버 미구현. 하단 네비에서 진입점 자체를 감추는 편이 낫다 |
| **5분 연장** | P0 | ⚠️ 아래 참고 — **CUT 대상이 아닐 수 있다** |

### E2E 자동화(A58, XL)는 CUT 을 제안한다

한 명 체제에서 **XL 자동화를 새로 짜는 비용이 수동 회귀보다 크다.**
대신 **매칭·세션 회귀 테스트(A32·A33)를 수동 체크리스트로 만들어 반복**하는 쪽을 권한다.
데모 전에 두 번 돌리면 자동화 없이도 안전망이 된다.

---

## 3. 조사에서 나온 정정 — 범위 판단이 바뀌는 것들

체크리스트를 그대로 믿고 범위를 정하면 틀린다. [BACKEND_DEPENDENCIES.md](BACKEND_DEPENDENCIES.md) 참고.

**5분 연장은 "백엔드 대기"가 아니다.** `POST /api/v1/sessions/{sessionId}/extensions` 가
이미 있고 STOMP 이벤트까지 나간다. 양측 `AGREE` 면 35분, 아니면 30분 종료로 규칙도 확정돼 있다.
현재 FE 토글은 실제 시간을 늘리지 못하는 **가짜 기능**이므로,
**연동(M)하거나 화면에서 내리거나** 둘 중 하나를 골라야 한다. 지금 상태가 제일 나쁘다.

**신고·차단, 차단 목록, 제재 조회도 이미 구현돼 있다.** "백엔드 대기"로 미뤄둘 이유가 없다.

**소셜 가입자는 매칭이 전혀 잡히지 않는다.** `birthDate` 가 비어 있어
`MatchEligibilityPolicy` 가 후보에서 제외한다. 조용히 실패해서 아무 로그도 남지 않는다.
→ **범위 논의와 무관하게 최우선 수정 대상.**

---

## 4. 제안 요약

| | 제안 |
|---|---|
| P0 재정의 | 32건 → **MUST 8건 내외**로 축소 |
| CUT | AI 화상 · 관리자 · 스타일링 · 성장 · 리포트 목록 · E2E 자동화 |
| 대안 확정 | 리포트가 안 오면 **'준비중' 화면으로 마감**한다고 못 박기 |
| 즉시 처리 | 소셜 가입자 매칭 불가 · 5분 연장 가짜 기능 |
| 회귀 전략 | E2E 자동화 대신 **수동 회귀 체크리스트** |

## 5. PO 결정이 필요한 것

1. 위 **CUT 목록을 승인**하는가? 특히 **AI 화상 트랙 제외**에 동의하는가?
2. **리포트가 제때 안 나오면 '준비중'으로 마감**하는 데 동의하는가?
3. **5분 연장**은 연동하는가, 화면에서 내리는가?
4. **관리자 화면**을 데모에서 보여줄 필요가 있는가?

결정되면 체크리스트의 우선순위 열을 MUST/SHOULD/CUT 기준으로 일괄 재조정한다.
